# Dependencies
node_modules/

# Runtime data & uploads — never commit residents'/applicants' personal data.
# In production these live in Supabase; locally they land in data/.
data/

# Secrets / environment
.env
.env.*
!.env.example
*.local

# Logs
*.log
npm-debug.log*

# Generated static preview (rebuild with build-static.ps1)
preview/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/*
!.vscode/launch.json

# Photos uploaded through the staff hub when running locally
public/images/uploads/
